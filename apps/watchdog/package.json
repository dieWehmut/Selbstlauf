{
  "name": "@ai-cli-bypass/watchdog",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build --silent && npm run test:compiled --",
    "test:compiled": "cd dist && node --test"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.0"
  }
}
