{
  "name": "@ai-cli-bypass/watchdog",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build --silent && node --test --test-concurrency=1 dist/test/*.test.js",
    "claude-stop-hook": "node dist/src/claude/stop-hook-cli.js"
  }
}
